<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <RootNamespace>SampleMcpServer</RootNamespace>
    <UserSecretsId>headless-agent-workflow-sample-mcp</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="ModelContextProtocol.AspNetCore" />
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" />
    <PackageReference Include="Microsoft.Identity.Web" />
  </ItemGroup>

  <ItemGroup>
    <!-- Only for the intake read-through (FileIntakeStore). A real land MCP server has no dependency on the workflow. -->
    <ProjectReference Include="..\AgentWorkflow.Core\AgentWorkflow.Core.csproj" />
  </ItemGroup>

</Project>
